# llamagputop
A zero-dependency Python script that provides a terminal interface for monitoring Linux hardware and llama.cpp instances. It reads sysfs, procfs, and driver data to display GPU metrics (AMD, Intel, NVIDIA) and auto-detects llama-server processes to show generation speed, KV-cache fill, draft head usage, and per-process VRAM consumption.
